# EXP-Q2-009

Mass and heat residual audit over 0–300 s. The reported values are residuals of the implemented discrete equations with variable `rho(C)cp(C)`; they are not upgraded to physical mechanism claims.
