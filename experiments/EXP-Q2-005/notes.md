# EXP-Q2-005

Time sensitivity with fixed uniform `dr=0.025 cm` (`n=80`) and BE only. `dt=1, 0.5, 0.25, 0.125 s` are compared against the smaller `dt=0.03125 s` reference. Errors are retained in full floating-point precision.
