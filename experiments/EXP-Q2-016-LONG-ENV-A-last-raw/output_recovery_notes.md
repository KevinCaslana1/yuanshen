The append-only files from an interrupted run were intentionally retained as raw evidence. Because a process advanced beyond checkpoint_43200s.json before the process was interrupted, resuming from that older checkpoint duplicated 43201--47824 s. `official_samples_recovered.csv` and `diagnostics_1s_recovered.csv` retain the first deterministic row for each official key; the raw duplicate counts and reason are recorded in `output_recovery_metrics.json`. The solver now truncates output/diagnostic files to the checkpoint cursor before restart.
