# EXP-Q2-NUM-REMEDY-C-TIME

This experiment is authorized only because the fixed-spatial temporal isolation showed that t=1 s surface moisture is time-dominated. It uses dt=0.015625 s through t=2 s, then restarts with BE at the step-size change and uses dt=0.25 s afterwards. No physical input, boundary value, property, or interface rule is changed.
