# EXP-Q2-PROPERTY-POINTS

这是 Q2 设计阶段的只读公式审计，不是 Q2 求解器实验。公式来源为官方 PDF 附录3；所有内部温度测试点使用 K。脚本只写本实验目录的 `metrics.json`，不修改 `A题/`，不生成 `result2.xlsx`，不产生论文数值结论。

结果解释：正值/有限性、`D(C,T)` 在选定正域测试点上的单调性以及 `C=0` 域保护均用于实现前检查；把 `50°C` 当作 `50 K` 的探针会得到极小的扩散系数，用于证明单位测试必须拒绝这种输入。
