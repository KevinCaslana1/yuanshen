# EXP-Q2-ENV-TAIL

这是附件1的只读尾窗统计。它登记原始最后点、最后 10/20/40/80 点的均值、总体标准差、范围和线性趋势，供 `OQ-Q2-ENV-001` 审查。团队建议的 `50.00°C/0.0500` 只作为待确认的 `TEAM_REFERENCE_NUMERICAL_PROPOSAL`，不是官方常值。

本实验不拟合边界、不生成插值数据、不运行 Q2 solver、不写任何官方模板或 `result2.xlsx`。
