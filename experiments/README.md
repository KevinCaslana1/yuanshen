# Experiments

每个正式实验使用唯一目录：`EXP-xxx/`。

建议包含：

- `config.json`：数据、特征、模型、参数和随机种子；
- `metrics.json`：评价指标与比较结果；
- `notes.md`：目的、运行方式、解释和限制；
- 必要时的 `predictions.csv`、`logs.txt`、`artifacts/`。

实验索引和关键结果登记在 `docs/EXPERIMENTS.md`，不要把大段运行日志复制进长期文档。

