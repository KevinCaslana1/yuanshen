# EXP-Q2-006

Space sensitivity with fixed `dt=0.125 s` and uniform grids `dr=0.1, 0.05, 0.025 cm`; `dr=0.0125 cm` is the reference. Comparisons use the same physical radii and time points, not same array indices.
