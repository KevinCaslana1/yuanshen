# Handoff

> 本文件保持很短，只服务于新会话、上下文压缩或其他 Agent 的快速接管。

## 当前做到哪里

Workflow 与 Git 已初始化；尚未开始赛题分析。

## 刚刚完成什么

创建了项目规范、状态/任务/证据记录模板和推荐目录；已在 `main` 创建本地初始提交。

## 当前最好结果

无真实实验结果。

## 当前主要决策

尚无正式建模决策。

## 不要重复尝试什么

尚无失败路线；不要在没有正式赛题和数据前虚构模型结果。

## 下一步

获取赛题与附件，阅读 `docs/STATE.md`、`docs/TODO.md` 和 `AGENTS.md` 后开始题目拆解。

## 重点阅读文件

`AGENTS.md`、`docs/STATE.md`、`docs/TODO.md`、`docs/EXPERIMENTS.md`、`docs/VALIDATION.md`。

## 风险与注意事项

当前目录已是 Git 仓库，`main` 上有本地初始提交；推送到 `origin` 因连接 GitHub 失败而待完成。`data/raw/` 初始化后必须保持只读。
